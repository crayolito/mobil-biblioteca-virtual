import 'package:flutter/material.dart';

class OptionsBook extends StatelessWidget {
  const OptionsBook({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
