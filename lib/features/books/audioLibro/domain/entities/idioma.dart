class Idioma {
    int id;
    String nombre;
    String codigo;
    DateTime createdAt;
    DateTime updatedAt;

    Idioma({
        required this.id,
        required this.nombre,
        required this.codigo,
        required this.createdAt,
        required this.updatedAt,
    });

}
