class Usuario {
    String name;
    String email;
    DateTime updatedAt;
    DateTime createdAt;
    int id;
    String profilePhotoUrl;

    Usuario({
        required this.name,
        required this.email,
        required this.updatedAt,
        required this.createdAt,
        required this.id,
        required this.profilePhotoUrl,
    });


}
